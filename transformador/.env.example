PORT=4321

# Un host por dominio
EJEMPLO_GRPC_HOST=localhost:50051
GRPC_DEADLINE_MS=3000

# Bus de pub/sub para el fan-out del SSE.
# Vacío = bus en memoria, que SOLO funciona con una instancia del transformador: con
# dos, un cliente conectado a A no recibe lo que llegó a B, en silencio.
